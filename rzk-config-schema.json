{
  "$schema": "http://json-schema.org/draft-07/schema",
  "properties": {
    "include": {
      "description": "The list of files to be typechecked (in order). Supports globs",
      "type": "array",
      "items": {
        "type": "string"
      }
    },
    "exclude": {
      "description": "The list of files to be excluded from typechecking",
      "type": "array",
      "items": {
        "type": "string"
      },
      "default": []
    }
  }
}
